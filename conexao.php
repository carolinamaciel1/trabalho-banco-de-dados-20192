<?php

@$link = mysqli_connect('localhost', 'root', '','sistemalojasql') or die("Não foi possível conectar o banco de dados. Verifique o phpmyadmin.");



	/*if (!$link) {
		die('Impossível conexão com o servidor: '.mysql_error());
	}
	@$db_selected = mysqli_select_db('sistemaloja', $link);
		if (!$db_selected) {
			die('Impossível conexão com o banco: '.mysql_error());
		}else{
  echo "conexão ok";
		}*/

?>